<div class="group-children block">
  <ul class="group-children--list">
    {% for child in children %}
      <li class="group-children--child">
        <a href="{{ child.link }}">{{ child.label }}</a>
      </li>
    {% endfor %}
  </ul>
</div>
