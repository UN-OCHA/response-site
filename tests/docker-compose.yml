version: "2.2"

networks:
  default:

volumes:
  hrinfo9-test-site-public:
  hrinfo9-test-site-private:

services:
  redis:
    restart: always
    image: public.ecr.aws/unocha/redis:5
    hostname: hrinfo9-test-redis
    container_name: hrinfo9-test-redis
    ports:
      - "6379"
    networks:
      - default

  mysql:
    image: public.ecr.aws/unocha/mysql:10.4.13-r0-202006-01
    hostname: hrinfo9-test-mysql
    container_name: hrinfo9-test-mysql
    environment:
      - MYSQL_DB=reliefweb
      - MYSQL_USER=reliefweb
      - MYSQL_PASS=reliefweb
    networks:
      - default

  drupal:
    image: public.ecr.aws/unocha/hrinfo9-site:local
    hostname: hrinfo9-test-site
    container_name: hrinfo9-test-site
    depends_on:
      - redis
      - mysql
    volumes:
      - "./settings:/srv/www/shared/settings:ro"
      # Mount volumes for the private and public files.
      - "hrinfo9-test-site-public:/srv/www/html/sites/default/files:rw"
      - "hrinfo9-test-site-private:/srv/www/html/sites/default/private:rw"
      # Mount the folders needed for the tests.
      - "../phpcs.xml:/srv/www/phpcs.xml:ro"
      - "../phpunit.xml:/srv/www/phpunit.xml:ro"
      - "../.coveralls.yml:/srv/www/.coveralls.yml:ro"
      # Mount local custom code.
      - "../html/modules/custom:/srv/www/html/modules/custom:ro"
      - "../html/themes/custom:/srv/www/html/themes/custom:ro"
    environment:
      - TERM=xterm
      - ENVIRONMENT=dev
      - NGINX_SERVERNAME=hrinfo9-test-site,localhost,127.0.0.1
      - NGINX_OVERRIDE_PROTOCOL=HTTP,hrinfo9-test-site,localhost,127.0.0.1
      - DRUSH_OPTIONS_URI=http://hrinfo9-test-site
      - DRUPAL_DB_DATABASE=hrinfo
      - DRUPAL_DB_USERNAME=hrinfo
      - DRUPAL_DB_PASSWORD=hrinfo
      - DRUPAL_DB_HOST=mysql
      - DRUPAL_DB_DRIVER=mysql
      - PHP_ENVIRONMENT=dev
      - PHP_XDEBUG=true
    networks:
      - default
